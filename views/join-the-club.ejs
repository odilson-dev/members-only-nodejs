<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Join the club</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('header') %>
    <div class="box-form">
      <h1>Hi <%= currentUser.first_name %>, join us</h1>
      <p>Enter the secret pass code to join the club</p>
      <% if(errors){ %> <%- include('errors', { errors }) %> <% }%>
      <form action="/users/join-the-club/<%= currentUser.id %>" method="POST">
        <div class="field">
          <input
            type="text"
            name="passCode"
            id="passCode"
            placeholder="Secret Pass Code"
          />
        </div>
        <button class="submit-btn" type="submit">Submit</button>
      </form>
    </div>
  </body>
</html>
